require('dotenv').config(); const pool = require('./src/config/db'); async function migrate() { try { console.log('Running status views migration...'); await pool.query('CREATE TABLE IF NOT EXISTS status_views (status_id INT REFERENCES statuses(id) ON DELETE CASCADE, viewer_id INT REFERENCES users(id) ON DELETE CASCADE, viewed_at TIMESTAMPTZ DEFAULT NOW(), PRIMARY KEY (status_id, viewer_id))'); console.log('Migration complete!'); process.exit(0); } catch (error) { console.error('Migration failed:', error); process.exit(1); } } migrate();
